{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability for the local WarpgateSH companion window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
