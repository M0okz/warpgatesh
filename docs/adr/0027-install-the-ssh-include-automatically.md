# Installer automatiquement la directive SSH

L’ajout du premier Profil Warpgate installe automatiquement la Directive d’inclusion si elle est absente, sans confirmation ni sauvegarde visible. Si `~/.ssh/config` ne peut pas être modifié, WarpgateSH le laisse intact et affiche simplement la ligne que l’utilisateur doit ajouter manuellement.
